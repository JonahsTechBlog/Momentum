
---

## 4) `pubspec.yaml`

```yaml
name: momentum
description: Offline workout tracker with custom presets, favorites, and history.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.15
  wakelock: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
